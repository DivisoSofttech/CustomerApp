import { ArrayFormaterPipe } from './array-formater.pipe';

describe('ArrayFormaterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayFormaterPipe();
    expect(pipe).toBeTruthy();
  });
});
